import React from "react";

const SQLStage = () => {
  return (
    <div>
      <h2>SQL Stage</h2>
      <p>Solve the challenges to move to the next stage!</p>
      {/* Add the challenge logic here somewhere */}
    </div>
  );
};

export default SQLStage;
